# 架构决策记录

用户完成 `docs/architecture-options.md` 的反选后，每个确认项写成独立 ADR（Architecture Decision Record，架构决策记录），至少包含：背景、最终选择、放弃方案、兼容性影响和可逆条件。

编号示例：

```text
0001-data-scope.md
0002-ingestion-sources.md
0003-process-topology.md
```

处于“提议”状态的 ADR 不能被实现当成既定约束；只有用户确认后才改为“已接受”。
