# 项目协作说明

本文档只记录不写就难以从仓库本身推断的约束；架构选择以 `docs/decisions` 中已接受的决策为准。

## 项目定位

- 本项目是通用 Bangumi 数据采集、规范化、查询与分发服务，不属于 neko-bot，也不内置 Wife 抽取业务。
- Wife 是首个消费者。消费者只能依赖稳定 API 或已发布的客户端，不得直接连接本服务数据库。
- 采集实现、上游字段变化和数据库 migration 属于服务内部细节，不应迫使消费者同步修改。
- 原始来源指标必须保留 `provider`、`metric`、来源时间和采集时间；不同指标不得直接相加或假装等价。

## 开发规范

- 技术栈使用 Kotlin、Gradle Kotlin DSL；代码注释、文档正文、错误信息和用户可见说明优先使用中文。
- 提交信息使用 Conventional Commits，例如 `feat(import): 支持 Archive 基线导入`。
- 不写死具体作品、角色或消费者规则；通用算法必须由数据特征和显式配置驱动。
- 已发布或已执行的版本化 migration 不得修改，只能新增更高版本。
- 修改已有文件前先检查 Git diff，避免覆盖其他任务的未提交改动。
- 引用社区项目时记录许可证；未决定项目许可证前不得复制第三方实现代码。

## 数据原则

- 官方 Bangumi Archive 适合建立非实时全量基线，Bangumi v0 API 适合近期补充、按需刷新和详情校验。
- 搜索接口属于实验能力，只用于发现候选，不作为实体真值来源。
- 上游原始关系和热度必须无损保存；角色熟悉度、人气层级等派生结果需要记录算法版本，允许重新计算。
- 数据库不得保存图片二进制；是否增加独立图片缓存或对象存储由后续决策确定。
- 全量同步由 `withSyncLock` 持有会话级 PostgreSQL advisory lock；数据代激活不得从另一连接重复申请同一把锁，否则会形成跨连接自锁死。激活事务只负责校验非空目录并原子切换活动数据代。
- Archive 实体与关系按 5000 条一批提交，同一来源摘要的未完成数据代必须复用；角色与作品详情通过 `enriched` 状态按 100 条一批断点续采。首次同步完成第一批有效角色后即可发布数据代，作品详情继续在活动数据代上补充；后续数据代在角色补充完成前继续保留旧活动代。
- 熟悉关系必须同时通过作品内角色相对热度、同类型全站角色绝对热度和同类型全站作品收藏热度的自适应分布筛选；不得只凭冷门作品内部排名纳入角色。角色接口只返回通过筛选的作品关系，并按作品收藏热度排序。Archive 的五项作品收藏状态原样保存，用受 token 保护的诊断接口区分冷门作品与作品内误选。
- 作品和角色缺少中文名时使用 Archive 或 v0 API 的原名，不得把中文名作为入选条件。NSFW 条目可以参与关系和热度计算，但 Bangumi 没有图片级安全字段；在逐图审核通过前不得向消费者下发其封面，角色自身仍按角色级 NSFW 标记过滤。
- 生产镜像把第三方运行库和项目 jar 拆成独立层；发布流水线使用最小 GHA 构建缓存，避免版本变化时重复上传和下载整套依赖。
